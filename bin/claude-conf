#!/bin/bash

set -e
set -x

# If EDITOR is not set, set it to cursor
if [ -z "$EDITOR" ]; then
    EDITOR="cursor"
fi

eval "$EDITOR" "$HOME/Library/Application\ Support/Claude/claude_desktop_config.json"
