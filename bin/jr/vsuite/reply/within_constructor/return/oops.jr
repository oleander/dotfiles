// doesn't exactly belong here under reply,
// but no other logical place for it
// and it is related to similar reply test.

import edu.ucdavis.jr.JR;
public class oops {
  private int balance = 0;
  {
      balance = 20000;
      if (balance > 0) return;
      balance = 888;
  }
  static {
      if (3 > 0) return;
  }
}
