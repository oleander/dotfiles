needs timeout program, which is written in C,
which we don't want to insist that users have.

Since we have a "self-timed" version of this test, we'll not use this
version.  But, keep it around for possible later use.
