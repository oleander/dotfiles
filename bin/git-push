#!env fish

git push origin -u $args

# if test $status -ne 0
#   echo "==> Push failed, try pulling"
#   git pull --rebase origin

#   if test $status -ne 0
#     echo "==> Pull failed, aborting"
#     exit 1
#   end

#   git push origin -u
# end
