#!/usr/bin/env ruby

# Git Push Origin, version 0.1, 2011-03-09
# Copyright (c) 2011, Linus Oleander
# Released under the MIT licens

# Pushes to origin {current} branch

`git push -u origin #{$1} --tags` if `git branch` =~ /\* ([^\s]+)/