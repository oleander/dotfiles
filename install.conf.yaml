- defaults:
    link:
      relink: true
      create: true
      force: true
      dirs: true

- clean: ~/

- link:
    ~/:
      path: configs/**/*
      glob: true
      prefix: "."
      exclude: [iterm]
