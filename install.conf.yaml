- defaults:
    link:
        relink: true

- clean:
    - '~'

- link:
    ~/:
        glob: true
        path: configs/*
        prefix: .

- shell:
    - git submodule update
    - sudo gem install hirb colorize overcommit bundler tty-prompt
    - sudo apt install safe-rm zsh git zsh
